package constants

const (
	BasePath              = "/"
	APIVersionPrefix      = "/dashboard/v1"
	RegistrationsEndpoint = APIVersionPrefix + "/registrations/"
	DashboardsEndpoint    = APIVersionPrefix + "/dashboards/"
	NotificationsEndpoint = APIVersionPrefix + "/notifications/"
	StatusEndpoint        = APIVersionPrefix + "/status/"
)
